{% if customer.id %}
    <a href="{{ url("store_user") }}" class="button-secondary mb-n">
        <i class="icon-user"></i>
        <strong>{{ customer.firstname }}</strong>
    </a>
    <a href="{{ url("store_logout") }}"><i class="icon-sign-out"></i></a>
{% else %}
    <a href="{{ url("store_login") }}" class="button-secondary mb-n">
        <i class="icon-sign-in"></i> {{ "template.BlueDay_bundle.user.nav.login"|trans }}
    </a>
{% endif %}